const PageEnum = {
  SiteWrapper: {
    // Can be better: Now you have different approaches to naming.
    // It would be better if you chose a single one.
    SEARCH: document.querySelector(`.columns__search-item`),
    rating: document.querySelector(`.columns__rating`)
  }
};
